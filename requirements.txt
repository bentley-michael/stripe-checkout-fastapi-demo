# Core app
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-dotenv>=1.0
python-multipart>=0.0.9

# PDFs (pure-Python path you’re using)
reportlab>=4.0
Pillow>=10.0

# Payments & optional email
stripe>=9.0
sendgrid>=6.10.0

# Optional: WeasyPrint (skip on Windows to avoid native deps)
weasyprint>=62.0; platform_system != "Windows"
